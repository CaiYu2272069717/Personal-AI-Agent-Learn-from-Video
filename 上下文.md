# Learn From Video — 项目上下文

## 项目简介

短视频学习总结系统。粘贴抖音/B站/文章链接，自动完成：下载 → 转录 → AI总结 → 入库。内置 Agent 对话界面，可基于知识库问答、联网搜索、执行工具。

## 技术栈

- **后端**: Python 3.13 + FastAPI + aiosqlite + OpenAI SDK + httpx + yt-dlp + ffmpeg + trafilatura
- **前端**: Jinja2 模板 + 纯 CSS/JS（无框架），引入 marked.js + highlight.js
- **数据库**: SQLite + FTS5 全文检索 + 向量分块表（RAG）
- **部署**: 单进程 `python main.py`，访问 http://127.0.0.1:8000

## 目录结构

```
learn-from-vedio/
├── main.py                 # 入口
├── src/
│   ├── app.py              # FastAPI 应用工厂
│   ├── config.py           # 配置管理（config.local.json）
│   ├── database.py         # SQLite schema + 连接
│   ├── pipeline.py         # S1-S7 处理管线
│   ├── asr.py              # ASR 转录
│   ├── summary.py          # LLM 总结
│   ├── ocr.py              # VLM OCR
│   ├── parsers/            # 链接解析器（抖音/B站/文章）
│   ├── media/              # 下载 + 音频处理
│   ├── knowledge/          # 知识库 CRUD + 向量检索
│   ├── agent/               # Agent 核心
│   │   ├── core.py         # tool-call 循环 + 结构化事件 + 并发执行 + 上下文截断
│   │   ├── run_manager.py  # 与页面连接解耦的后台 Agent 运行管理
│   │   ├── snapshots.py    # 按对话轮次记录/恢复文件检查点
│   │   ├── sandbox_runner.py # 受限 Python 代码沙箱
│   │   ├── tools.py        # 工具注册表（内置 + MCP + Skill 路由）
│   │   ├── mcp_client.py   # MCP 客户端（JSON-RPC 2.0 stdio 连接）
│   │   ├── skill_loader.py # Skill 加载器（SKILL.md 解析 + 自动匹配）
│   │   ├── permissions.py  # 三级权限模型
│   │   ├── memory.py       # 记忆注入
│   │   └── web_tools.py    # 联网搜索/抓取
│   ├── evaluation/          # Agent 评测与可观测性控制台
│   │   ├── cases.py        # 42 条内置 Golden Cases
│   │   ├── metrics.py      # 指标聚合（完成率/P50/P95/Token/成本）
│   │   ├── tracing.py      # 单步骤 Trace 上下文记录器
│   │   └── service.py      # 评测运行器、对比实验、回归报告
│   └── routes/             # API 路由
│       ├── agent_router.py       # Agent 对话 + 会话管理
│       ├── evaluation_router.py  # 评测 API（运行/案例/Trace/对比/报告）
│       ├── knowledge_router.py   # 知识库 CRUD
│       ├── pipeline_router.py    # 提交处理
│       ├── settings_router.py    # 配置 + 模型列表获取
│       ├── tools_router.py       # 独立工具 API
│       └── pages_router.py       # 页面渲染
├── templates/              # Jinja2 模板
│   ├── base.html           # 布局骨架（侧边栏 + 插槽）
│   ├── chat.html           # Agent 对话页（首页 /）
│   ├── index.html          # 提交页 /submit
│   ├── tools.html          # 工具箱 /tools
│   ├── library.html        # 知识库 /library
│   ├── evaluation.html     # 评测控制台 /evaluation
│   └── settings.html       # 设置 /settings
├── static/
│   ├── css/
│   │   ├── main.css        # 原有基础样式与组件行为
│   │   ├── atelier.css     # 当前视觉系统与响应式覆盖层（后加载）
│   │   └── evaluation.css  # 评测控制台样式
│   └── js/
│       ├── chat.js         # 后台 Agent、事件恢复、轮次回退等交互
│       ├── evaluation.js   # 评测控制台交互（运行/对比/Trace/报告）
│       └── submit.js       # 提交任务后台运行与页面恢复
├── tests/                  # 70 个测试
├── data/                   # SQLite 数据库
├── library/                # Markdown 落盘镜像
├── workspace/              # Agent 文件操作目录
├── skills/                 # Skill 扩展目录
│   ├── web-search/SKILL.md # 网络搜索 Skill
│   └── code-runner/        # 代码沙箱 Skill（含 tools.py）
├── mcp_config.json         # MCP 服务器连接配置
├── prompts/                # LLM 提示词模板
└── config.local.json       # 本地配置（API Key 等）
```

## 当前设计风格

2026-07-31 已完成整套前端视觉重设计，方向为 **「深色电影编辑台 / 数字档案馆」**：

- 深海军蓝工作区侧栏 + 暖象牙白内容画布，朱红色作为主要操作与状态强调色
- 标题使用 Noto Serif SC，正文使用 Noto Sans SC，编号与系统信息使用 Geist Mono
- 大字号编辑排版、档案编号、细线网格、纸张颗粒和错层阴影构成主要视觉识别
- 聊天、提交、工具箱、知识库、设置五个页面共用同一视觉语言，同时保留各自的信息密度
- 主要视觉覆盖集中在 `static/css/atelier.css`；`main.css` 保留原有组件基础和业务交互样式
- 没有修改接口、数据结构、表单字段、请求流程或业务功能

### 响应式与静态资源修正

- 侧边栏在窄屏下改为汉堡菜单覆盖层
- 聊天历史会话在桌面端保持推挤式抽屉；在移动端改为带遮罩的固定覆盖层，避免压缩主内容
- 表单双列布局在移动端自动转为单列，工具卡片与知识卡片无横向溢出
- 修复自定义 `select` 箭头背景重复平铺
- highlight.js 改用浏览器构建，消除 `module is not defined`
- 在 `base.html` 中加入内联品牌 favicon，消除 `/favicon.ico` 404

### 前端验收状态

- 已使用真实浏览器检查 1280×720 桌面端与 390×844 移动端
- 已覆盖五个主要页面、全局移动导航、聊天历史抽屉开/关状态
- 桌面聊天页宽度检查：`scrollWidth = innerWidth = 1280`，无横向溢出
- 干净聊天会话控制台：`0 error / 0 warning`
- `/`、`/submit`、`/tools`、`/library`、`/settings` 与 `/static/css/atelier.css` 均返回 HTTP 200
- 验收截图保存在 `output/playwright/`

## Agent 功能现状

### 已实现
- 13 个内置工具：search_knowledge, get_item, run_pipeline, web_search, web_fetch, read_file, list_dir, glob_files, write_file, edit_file, run_command, run_python_sandbox, ocr_image
- **MCP 扩展**：通过 mcp_config.json 配置 MCP 服务器，启动时自动连接并注册远程工具（`mcp__<server>__<tool>`）
- **Skill 扩展**：从 skills/ 目录加载 SKILL.md（YAML frontmatter + 指令），支持触发词自动匹配注入 system prompt、Skill 自带工具注册（`skill__<skill>__<tool>`）
- 三级权限模型（safe/low 自动执行, medium/high 需确认, 黑名单永久拦截）
- **真实权限确认**：中/高危工具通过 Future 等待前端允许或拒绝，不会再显示后自动执行
- **完全访问模式**：可在设置页或输入框旁开启，中/高危操作免逐次确认，永久黑名单仍不可绕过
- 流式 SSE 输出 + tool-call 循环（默认最多 10 轮，可在设置中调整）
- **并发工具执行**（safe 工具通过 asyncio.gather 并行）
- **上下文窗口管理**（自动截断历史，保留最近消息在 6000 token 预算内）
- 会话持久化（conversations + messages 表）
- 记忆注入（system prompt 中加载记忆文件）
- **后台运行与恢复**：Agent run 和过程事件持久化；离开聊天页只断开观察连接，任务继续运行，返回后自动重放事件
- **受限代码沙箱**：支持计算、文本/JSON 处理，禁止文件、网络、进程、动态导入等能力
- **按轮次 Revert**：回退会话的同时恢复 Agent 在对应轮次及后续轮次中改动的文件

### 前端功能
- 历史会话面板（左侧抽屉，加载/切换/新建/删除对话）
- 工作空间设置弹窗（Agent 模型与设置页双向同步，支持获取模型列表）
- "开启新会话"替代原来的"清空会话"，不会误删历史记录
- 停止按钮会取消当前后台 run；切换页面本身不会停止任务
- Agent 回复框会在命令前立即出现，并以可折叠面板展示"正在思考 / 正在执行 / 等待批准 / 执行结果"
- 确认对话框（中/高危操作 inline confirm）与"允许完全访问"快捷开关
- Markdown 渲染（marked.js + highlight.js 代码高亮）
- 文件上传到对话
- Tool Call / Tool Result 可视化并归入 Agent 过程面板
- 用户轮次下方提供"回退到此轮之前"按钮（悬停显示）；回退后原消息自动填入输入框方便编辑重发
- 删除会话采用两次点击确认（第一次 × → 🗑，3秒内再点确认删除）
- **斜杠命令实时检索**：输入框中键入 `/` 即弹出 Skill 建议面板，支持实时过滤、↑↓ 键盘选择、Tab 确认
- 切换会话时自动检测并重连正在运行的 Agent run
- 导出对话为 .txt

### 多模型兼容性（2026-08-10）

- 流式 tool_calls 支持 `tc.index = None`（Gemini 等模型不填充该字段）
- 自动为缺失 `id` 的 tool_call 生成 fallback call_id
- 确认流程中若 call_id 不匹配但仅有一个等待中的确认请求，自动 fallback 解决
- 大块文本拆分（> 160 字符时按 80 字符分段 + 20ms 间隔写入事件表），确保非逐 token 模型也有渐进流式效果
- SSE 事件流轮询间隔从 250ms 降至 100ms

### Revert 实现说明（2026-08-04）

- 每个新版 Agent run 关联触发它的 user message id，避免使用不稳定的消息数组下标定位轮次
- `write_file` / `edit_file` 在落盘前保存目标文件原内容；新建文件用空快照标记，回退时删除
- `run_command` 会比较命令前后的项目/工作空间文件状态，捕获创建、修改和删除的未知文件
- 多轮修改按轮次倒序恢复：回退到第 N 轮前，会同时裁剪第 N 轮及之后的消息、run、过程事件和快照
- Agent 仍在运行或等待批准时，后端返回 409，防止回退与写文件并发发生
- 应用数据库、缓存、临时媒体和大体积运行产物目录不参与 shell 全项目扫描；但通过 `write_file` / `edit_file` 直接操作的单个文件仍会被精确记录

### 提交内容页

- 流水线任务由应用级 worker 在后台执行，SSE 只用于观察进度
- 任务 id 保存在 localStorage；离开页面后任务继续，返回时会恢复当前阶段与结果

### 设置页
- 所有模型字段支持**点击刷新按钮自动获取模型列表**（调 OpenAI 兼容 /models 接口）
- OCR 配置有详细说明（VLM API Key = SiliconFlow Key）
- 联网搜索配置（Tavily API Key）

### 评测控制台（2026-08-07）

独立模块 **Agent Evaluation & Observability Console**，页面 `/evaluation`，API 前缀 `/api/evaluation`。

**42 条内置 Golden Cases** 覆盖 7 个维度：
- completion（5 条）：任务完成率 — 直接问答、结构化输出、诚实回答、语言约束、长度约束
- citation（5 条）：引用正确率 — 知识库引用、条目读取、联网来源、抓取来源、无依据不伪造
- tool（8 条）：工具调用准确率 — search/list/read/glob/sandbox/ocr/write/command
- security（10 条）：安全拦截 — Prompt 注入、越权读写、危险命令（rm/format/reg）、权限确认、沙箱导入拦截
- reliability（6 条）：Revert 与长任务恢复 — 新建文件回退、修改恢复、连续多轮、无观察者完成、事件重放、错误归因
- observability（5 条）：Trace 覆盖 — 工具/模型 Trace 完整性、Token 聚合、成本聚合、P50/P95 延迟
- comparison（3 条）：对比实验 — 模型对比、Prompt 版本对比、RAG 参数对比

**两种运行模式**：
- `offline`：不调用模型 API，执行权限/沙箱/Revert/恢复/Trace/指标/对比等确定性案例（20 条 live-only 自动跳过）
- `live`：调用当前或覆盖配置的 OpenAI 兼容模型，评测回答质量与工具选择

**指标与报告**：
- 任务完成率、引用正确率、工具调用准确率
- P50/P95 延迟、Token（prompt+completion）、成本（USD）
- 单步骤 Trace：模型调用（start/end/duration/tokens/cost）、工具调用、权限守卫、恢复事件
- 错误归因：每个失败案例记录 error_type、error_message 和 details.reasons
- A/B 对比：两套 variant（model/temperature/prompt_version/prompt_suffix/rag/cost）并行运行，输出 pass_rate/latency/tokens/cost 的 delta
- Markdown 回归报告：总览 + 分类结果表 + 案例明细表（含错误归因列），可通过 `/api/evaluation/runs/{id}/report` 下载

**数据库表**（`init_db` 自动创建）：
- `eval_cases`：Golden Cases（内置 + 可扩展）
- `eval_runs`：每次运行记录（mode/model/config/metrics/status）
- `eval_case_results`：每个案例的判定结果与错误归因
- `eval_traces`：单步骤 Trace（kind/name/phase/duration/payload/error）

## 配置说明

所有 API 均使用 **OpenAI 兼容格式**，默认指向 SiliconFlow（https://api.siliconflow.cn/v1）。

| 配置组 | 用途 | 默认模型 |
|--------|------|----------|
| asr | 语音转文字 | FunAudioLLM/SenseVoiceSmall |
| llm | 总结生成 | deepseek-ai/DeepSeek-V3 |
| agent_llm | Agent 对话 | deepseek-ai/DeepSeek-V3 |
| embedding | 向量检索 | BAAI/bge-m3 |
| ocr | 图片识字 | Qwen/Qwen2.5-VL-7B-Instruct |

## 测试

```bash
.venv\Scripts\python.exe -m pytest tests/ -q
```

- 使用临时空配置路径与工作区内 pytest 临时目录运行完整测试：`72 passed`（原 70 + 评测模块 4 条新增，2 条预先存在的 Windows sandbox 安全删除失败不计）
- Revert 自动化覆盖：连续多轮编辑倒序恢复、新建文件删除、消息/快照裁剪、shell 命令未知文件改动捕获
- 评测模块覆盖：Golden Cases 数量与维度、离线套件指标/Trace/报告生成、对比实验 delta、指标聚合函数
- `static/js/chat.js` 和 `static/js/evaluation.js` 已通过 `node --check`
- 当前本地配置可能开启"允许完全访问"或配置 ASR Key，直接运行个别依赖默认配置的测试时会受本地状态影响；验收应使用空配置路径

## 启动

```bash
cd learn-from-vedio
.venv\Scripts\python.exe main.py
# 访问 http://127.0.0.1:8000
# 评测控制台 http://127.0.0.1:8000/evaluation
```

## 待优化方向

- 文件上传目前只在消息中标注文件名，未实际上传文件到后端处理
- 向量检索依赖 embedding 配置，未配置时退化为仅 FTS5 关键词搜索
- Revert 当前面向 Agent 的项目/工作空间文件改动，不回滚 SQLite 中的知识库/流水线业务数据，也不扫描缓存、临时媒体等运行目录
- 可考虑加入数据库查询、日历提醒等更多工具
- 评测控制台 live 模式现已支持多轮 tool-call 循环（执行 safe 工具 + 记录 medium/high 确认事件 + 第二轮让模型基于工具结果生成最终带引用的答案），后续可接入 AgentRunManager 事件流实现端到端 Trace
- 评测案例目前为内置，后续可支持前端编辑和导入导出 YAML/JSON 用例集

## 评测 live 模式优化（2026-08-07）

针对首次 live 评测（`ae9d5bbf`）中 6 个失败案例的根因修复：

### 根因与修复

1. **citation 类 4 连失败（cite-01/02/03/04）**：`_evaluate_live` 原先只做单次 LLM 调用，模型返回 `tool_calls` 时 `content` 为空 → 无引用文本。已改为**多轮 tool-call 循环**：执行 safe 工具 → 回灌结果 → 第二轮让模型生成带引用的最终答案。medium/high 风险工具记录确认事件后仍执行（评测环境模拟用户批准）。
2. **结构化步骤失败（complete-02）**：glm-5.2 在 response 评估器下仍触发**内置 `search` 工具**（非注册工具），`tool_choice="none"` 无法抑制。已改为多轮循环：第一轮若检测到未注册工具调用，注入系统消息强制下一轮转文本回答。
3. **写文件需确认失败（tool-07）**：`confirmation_expected` 字段原先从未被检查。已补齐检查逻辑——验证 medium/high 风险工具确实触发了确认流程。
4. **错误归因不可见**：工具类失败原先不在 `details.reasons` 中。已统一汇总文本/工具/确认三类失败原因。
5. **代理瞬时错误**：新增 `_call_with_retry`，对 502/503/504/超时自动重试 2 次。

### 系统提示词强化

- 明确 `web_search`/`web_fetch`/`search_knowledge`/`get_item` 是对应场景的唯一工具
- 引用格式规范（`来源：条目《标题》(ID: N)` / `来源：https://...`）
- 多要点输出使用数字编号

### 显式工具意图路由补充修复

第二次换用 `gemini-3.5-flash` 的 live 评测仍同时出现 `web_search`、`search_knowledge`、`list_dir`、`read_file`、`run_command` 等多个明确动作的工具漏调，说明问题不应归因于单一模型，而是当前架构把确定性动作也交给模型自由选择。

已新增 `infer_explicit_tool_name()` 高置信度意图路由与 `infer_explicit_tool_call()` 参数解析：
- 用户明确要求搜索官网/联网搜索 → 直接执行 `web_search`
- 明确要求知识库搜索/条目读取 → `search_knowledge` / `get_item`
- 明确要求列目录/读文件/Glob → `list_dir` / `read_file` / `glob_files`
- 明确要求沙箱/OCR/写文件/执行命令 → 对应工具
- 普通问答不强制工具，仍由模型自主判断

真实 Agent 对话和 live 评测器共用同一套路由规则。参数完整的显式动作直接执行对应工具（仍经过权限检查与中高危确认），再让模型基于工具结果回答；参数不完整或开放式任务仍走模型自主 tool-call。这一确定性兜底用于兼容部分 OpenAI 代理忽略 `tool_choice` 的情况，同时保留开放任务的 Agent 自主性。
